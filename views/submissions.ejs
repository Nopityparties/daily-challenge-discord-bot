<!DOCTYPE html>
<html>
<head><title>Submissions</title></head>
<body>
  <h1>Submissions by Role</h1>
  <table border="1">
    <tr><th>Username</th><th>Tier</th><th>Count</th></tr>
    <% Object.values(userSubs).forEach(u => { %>
    <tr><td><%= u.username %></td><td><%= u.tier %></td><td><%= u.count %></td></tr>
    <% }); %>
  </table>
  <a href="/">← Back</a>
</body>
</html>